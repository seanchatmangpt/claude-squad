{
  "metadata": {
    "title": "TOP 10 Claude Code Skills Patterns (2025)",
    "version": "1.0",
    "date": "2025-12-28",
    "sources": [
      "https://code.claude.com/docs/en/skills",
      "https://platform.claude.com/docs/en/agents-and-tools/agent-skills/overview",
      "https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices",
      "https://www.anthropic.com/engineering/equipping-agents-for-the-real-world-with-agent-skills",
      "https://github.com/anthropics/skills",
      "https://github.com/travisvn/awesome-claude-skills",
      "https://leehanchung.github.io/blogs/2025/10/26/claude-skills-deep-dive/",
      "https://mikhail.io/2025/10/claude-code-skills/"
    ]
  },
  "skills": [
    {
      "name": "tdd-workflow",
      "description": "Implements test-driven development workflow by writing failing tests first, then implementing code to make tests pass, ensuring comprehensive test coverage for new features and bug fixes",
      "skill_template": "---\nname: tdd-workflow\ndescription: Implements test-driven development workflow by writing failing tests first, then implementing code to make tests pass, ensuring comprehensive test coverage for new features and bug fixes\nallowed-tools:\n  - Read\n  - Write\n  - Edit\n  - Bash\n  - Grep\n  - Glob\n---\n\n# Test-Driven Development Workflow\n\nThis skill implements the TDD red-green-refactor cycle for robust software development.\n\n## Workflow\n\n1. **Understand Requirements**: Analyze the feature or bug fix requirements\n2. **Write Failing Test (RED)**: Create a test that defines expected behavior\n3. **Run Test**: Verify the test fails for the right reason\n4. **Implement Code (GREEN)**: Write minimal code to make the test pass\n5. **Run Test Again**: Verify the test now passes\n6. **Refactor**: Improve code quality while keeping tests green\n7. **Commit**: Commit the feature with tests\n\n## Test Structure\n\n- Use descriptive test names: `test_<feature>_<scenario>_<expected_result>`\n- Arrange-Act-Assert pattern\n- One assertion per test when possible\n- Test edge cases and error conditions\n\n## Best Practices\n\n- Write the simplest test that could possibly fail\n- Run tests frequently (after each small change)\n- Keep tests independent and isolated\n- Mock external dependencies\n- Aim for high code coverage (>80%)\n\n## Example\n\n```python\n# Step 1: Write failing test\ndef test_calculate_total_with_discount_applies_percentage():\n    cart = ShoppingCart()\n    cart.add_item(Item(\"Book\", 100))\n    total = cart.calculate_total(discount_percent=10)\n    assert total == 90\n\n# Step 2: Run test (FAILS - calculate_total doesn't exist)\n\n# Step 3: Implement minimal code\nclass ShoppingCart:\n    def calculate_total(self, discount_percent=0):\n        subtotal = sum(item.price for item in self.items)\n        return subtotal * (1 - discount_percent / 100)\n\n# Step 4: Run test (PASSES)\n\n# Step 5: Refactor if needed\n```\n\n## Commands to Run\n\n- Python: `pytest -v`\n- JavaScript: `npm test` or `jest`\n- Go: `go test ./...`\n- Rust: `cargo test`\n\n## When to Use This Skill\n\nActivate when:\n- User requests \"write tests first\"\n- User mentions TDD, test-driven, or red-green-refactor\n- User asks to add a new feature with tests\n- User wants to fix a bug with test coverage",
      "trigger_keywords": [
        "TDD",
        "test-driven development",
        "write tests first",
        "red-green-refactor",
        "failing test",
        "test coverage",
        "unit tests"
      ],
      "allowed_tools": [
        "Read",
        "Write",
        "Edit",
        "Bash",
        "Grep",
        "Glob"
      ]
    },
    {
      "name": "semantic-commit",
      "description": "Generates clear, semantic commit messages following conventional commits format by analyzing git diffs and creating structured messages with type, scope, and description for better project history",
      "skill_template": "---\nname: semantic-commit\ndescription: Generates clear, semantic commit messages following conventional commits format by analyzing git diffs and creating structured messages with type, scope, and description for better project history\nallowed-tools:\n  - Bash\n  - Read\n  - Grep\n---\n\n# Semantic Commit Message Generator\n\nGenerates commit messages following the Conventional Commits specification.\n\n## Format\n\n```\n<type>(<scope>): <subject>\n\n<body>\n\n<footer>\n```\n\n## Types\n\n- **feat**: New feature for the user\n- **fix**: Bug fix for the user\n- **docs**: Documentation changes\n- **style**: Formatting, missing semicolons, etc.\n- **refactor**: Code restructuring without behavior change\n- **perf**: Performance improvements\n- **test**: Adding or updating tests\n- **build**: Build system or dependencies\n- **ci**: CI configuration changes\n- **chore**: Other changes that don't modify src or test files\n\n## Rules\n\n1. **Subject Line**:\n   - Maximum 50 characters\n   - Lowercase, no period at end\n   - Imperative mood (\"add\" not \"added\")\n   - Describe what the commit does, not what you did\n\n2. **Body** (optional):\n   - Wrap at 72 characters\n   - Explain the \"why\" and \"what\", not the \"how\"\n   - Separate from subject with blank line\n\n3. **Footer** (optional):\n   - Breaking changes: `BREAKING CHANGE: description`\n   - Issue references: `Closes #123, #456`\n\n## Workflow\n\n1. Run `git diff --staged` to analyze changes\n2. Identify files and functions modified\n3. Determine commit type based on changes\n4. Extract scope from file paths or modules\n5. Generate concise subject line\n6. Add body for complex changes\n7. Add footer for breaking changes or issue links\n\n## Examples\n\n```\nfeat(auth): add OAuth2 authentication support\n\nImplements OAuth2 flow with Google and GitHub providers.\nAdds new AuthService class and configuration options.\n\nCloses #234\n```\n\n```\nfix(api): prevent race condition in user creation\n\nAdded mutex lock around user registration to prevent\nduplicate accounts when multiple requests arrive simultaneously.\n```\n\n```\nperf(database): optimize query performance with indexes\n\nBREAKING CHANGE: Requires database migration to add indexes.\nRun `npm run migrate` before deploying.\n```\n\n## When to Use\n\nActivate when:\n- User asks to \"create a commit\"\n- User mentions \"commit message\"\n- User says \"commit these changes\"\n- User requests \"conventional commits\"",
      "trigger_keywords": [
        "commit message",
        "semantic commit",
        "conventional commits",
        "git commit",
        "commit changes",
        "create commit"
      ],
      "allowed_tools": [
        "Bash",
        "Read",
        "Grep"
      ]
    },
    {
      "name": "debugging-assistant",
      "description": "Systematically debugs code issues using scientific method: reproducing the bug, forming hypotheses, adding strategic logging, analyzing stack traces, and testing fixes incrementally",
      "skill_template": "---\nname: debugging-assistant\ndescription: Systematically debugs code issues using scientific method by reproducing the bug, forming hypotheses, adding strategic logging, analyzing stack traces, and testing fixes incrementally\nallowed-tools:\n  - Read\n  - Write\n  - Edit\n  - Bash\n  - Grep\n  - Glob\n---\n\n# Systematic Debugging Assistant\n\nApplies scientific debugging methodology to identify and fix bugs efficiently.\n\n## Debugging Process\n\n### 1. Reproduce the Bug\n- Create minimal reproducible example\n- Document exact steps to trigger the issue\n- Note expected vs actual behavior\n- Capture error messages and stack traces\n\n### 2. Form Hypotheses\n- Based on error messages\n- Based on recent changes (git log)\n- Based on similar past issues\n- List hypotheses in order of likelihood\n\n### 3. Gather Evidence\n- Add strategic logging/print statements\n- Use debugger breakpoints\n- Inspect variable states\n- Check system resources (memory, CPU, disk)\n- Review relevant documentation\n\n### 4. Test Hypotheses\n- Test one hypothesis at a time\n- Make minimal changes\n- Verify each change\n- Keep notes on what was tried\n\n### 5. Implement Fix\n- Apply the minimal fix that addresses root cause\n- Add test to prevent regression\n- Remove debug code\n- Document the fix\n\n## Common Bug Categories\n\n### Logic Errors\n- Off-by-one errors\n- Incorrect conditionals\n- Wrong operator (== vs ===, & vs &&)\n- Edge case handling\n\n### State Issues\n- Uninitialized variables\n- Mutating shared state\n- Race conditions\n- Stale closures\n\n### Integration Issues\n- API contract mismatches\n- Version incompatibilities\n- Missing dependencies\n- Environment differences\n\n### Performance Issues\n- Memory leaks\n- N+1 queries\n- Unbounded loops\n- Blocking operations\n\n## Debugging Tools by Language\n\n### Python\n```python\nimport pdb; pdb.set_trace()  # Breakpoint\nimport traceback; traceback.print_exc()  # Stack trace\nimport logging; logging.basicConfig(level=logging.DEBUG)\n```\n\n### JavaScript\n```javascript\ndebugger;  // Breakpoint\nconsole.trace();  // Stack trace\nconsole.log(JSON.stringify(obj, null, 2));  // Deep inspection\n```\n\n### Go\n```go\nimport \"log\"; log.Printf(\"%+v\", obj)  // Detailed output\nimport \"runtime/debug\"; debug.PrintStack()  // Stack trace\n```\n\n## Strategic Logging\n\n1. **Entry/Exit Points**: Log function calls with arguments and return values\n2. **State Transitions**: Log before/after critical state changes\n3. **Error Boundaries**: Log errors with full context\n4. **Performance**: Log timing for slow operations\n\n```python\ndef process_order(order_id):\n    logger.debug(f\"Processing order {order_id}\")\n    order = fetch_order(order_id)\n    logger.debug(f\"Order data: {order}\")\n    \n    try:\n        result = validate_order(order)\n        logger.debug(f\"Validation result: {result}\")\n    except ValidationError as e:\n        logger.error(f\"Validation failed for {order_id}: {e}\", exc_info=True)\n        raise\n    \n    logger.info(f\"Successfully processed order {order_id}\")\n    return result\n```\n\n## When to Use This Skill\n\nActivate when:\n- User reports a bug or error\n- User asks to \"debug\" or \"fix\" an issue\n- User provides stack trace or error message\n- User says something \"isn't working\"\n- User asks \"why is this failing?\"",
      "trigger_keywords": [
        "debug",
        "bug",
        "error",
        "not working",
        "failing",
        "fix issue",
        "stack trace",
        "troubleshoot"
      ],
      "allowed_tools": [
        "Read",
        "Write",
        "Edit",
        "Bash",
        "Grep",
        "Glob"
      ]
    },
    {
      "name": "code-review",
      "description": "Performs comprehensive code review checking for correctness, security, performance, maintainability, and best practices, providing actionable feedback with examples and severity ratings",
      "skill_template": "---\nname: code-review\ndescription: Performs comprehensive code review checking for correctness, security, performance, maintainability, and best practices, providing actionable feedback with examples and severity ratings\nallowed-tools:\n  - Read\n  - Grep\n  - Glob\n  - Bash\n---\n\n# Comprehensive Code Review\n\nProvides thorough, constructive code review following industry best practices.\n\n## Review Checklist\n\n### 1. Correctness\n- [ ] Does the code do what it's supposed to do?\n- [ ] Are edge cases handled?\n- [ ] Are errors handled properly?\n- [ ] Are there potential null/undefined access issues?\n- [ ] Are there off-by-one errors?\n\n### 2. Security\n- [ ] Input validation and sanitization\n- [ ] SQL injection prevention\n- [ ] XSS protection\n- [ ] Authentication and authorization\n- [ ] Sensitive data exposure\n- [ ] Dependency vulnerabilities\n\n### 3. Performance\n- [ ] Time complexity acceptable?\n- [ ] Space complexity acceptable?\n- [ ] Unnecessary computations?\n- [ ] Database query optimization\n- [ ] Caching opportunities\n- [ ] Memory leaks\n\n### 4. Maintainability\n- [ ] Code is readable and clear\n- [ ] Appropriate naming conventions\n- [ ] Functions are single-purpose\n- [ ] DRY principle followed\n- [ ] Magic numbers avoided\n- [ ] Comments where needed (not obvious code)\n\n### 5. Testing\n- [ ] Unit tests present and comprehensive\n- [ ] Integration tests for critical paths\n- [ ] Test names are descriptive\n- [ ] Edge cases tested\n- [ ] Mocks used appropriately\n\n### 6. Best Practices\n- [ ] Follows language idioms\n- [ ] Consistent with codebase style\n- [ ] Dependencies up to date\n- [ ] Documentation updated\n- [ ] No commented-out code\n- [ ] Git commits are logical\n\n## Feedback Format\n\n```\n**[SEVERITY] Category: Issue Title**\n\nFile: `path/to/file.ext:line`\n\n**Issue**: Clear description of the problem\n\n**Why it matters**: Impact explanation\n\n**Suggestion**: Specific fix with code example\n\n**Example**:\n```language\n// Before (problematic)\ncode example\n\n// After (improved)\ncode example\n```\n```\n\n## Severity Levels\n\n- **🔴 CRITICAL**: Security vulnerability, data loss risk, crashes\n- **🟡 HIGH**: Performance issue, major bug, incorrect behavior\n- **🟢 MEDIUM**: Maintainability, code smell, minor bug\n- **🔵 LOW**: Style, naming, documentation, nice-to-have\n\n## Review Principles\n\n1. **Be Constructive**: Focus on the code, not the person\n2. **Be Specific**: Point to exact lines and provide examples\n3. **Be Actionable**: Suggest concrete improvements\n4. **Prioritize**: Not all feedback is equally important\n5. **Praise Good Code**: Acknowledge well-written sections\n\n## Example Review Comment\n\n```\n**🟡 HIGH Security: SQL Injection Vulnerability**\n\nFile: `api/users.py:45`\n\n**Issue**: User input is directly interpolated into SQL query\n\n**Why it matters**: Attackers can inject malicious SQL to access or modify data\n\n**Suggestion**: Use parameterized queries\n\n**Example**:\n```python\n# Before (vulnerable)\nquery = f\"SELECT * FROM users WHERE email = '{email}'\"\nresults = db.execute(query)\n\n# After (secure)\nquery = \"SELECT * FROM users WHERE email = ?\"\nresults = db.execute(query, (email,))\n```\n```\n\n## When to Use This Skill\n\nActivate when:\n- User asks to \"review this code\"\n- User mentions \"code review\" or \"PR review\"\n- User asks \"is this code good?\"\n- User requests \"feedback on my code\"\n- User asks to \"check for issues\"",
      "trigger_keywords": [
        "code review",
        "review code",
        "PR review",
        "check code",
        "feedback",
        "code quality",
        "review changes"
      ],
      "allowed_tools": [
        "Read",
        "Grep",
        "Glob",
        "Bash"
      ]
    },
    {
      "name": "refactoring-guide",
      "description": "Guides safe refactoring by identifying code smells, suggesting design patterns, extracting duplicated code, improving naming, and ensuring tests pass at each step",
      "skill_template": "---\nname: refactoring-guide\ndescription: Guides safe refactoring by identifying code smells, suggesting design patterns, extracting duplicated code, improving naming, and ensuring tests pass at each step\nallowed-tools:\n  - Read\n  - Write\n  - Edit\n  - Bash\n  - Grep\n  - Glob\n---\n\n# Safe Refactoring Guide\n\nGuides systematic code refactoring while maintaining functionality.\n\n## Refactoring Process\n\n1. **Ensure Tests Exist**: Never refactor without tests\n2. **Run Tests**: Verify all tests pass (GREEN)\n3. **Make Small Change**: One refactoring technique at a time\n4. **Run Tests Again**: Ensure tests still pass\n5. **Commit**: Commit each successful refactoring step\n6. **Repeat**: Continue with next refactoring\n\n## Code Smells to Address\n\n### Bloaters\n- **Long Method**: Methods with 20+ lines\n- **Large Class**: Classes with too many responsibilities\n- **Long Parameter List**: More than 3-4 parameters\n- **Primitive Obsession**: Using primitives instead of objects\n\n### Object-Orientation Abusers\n- **Switch Statements**: Prefer polymorphism\n- **Temporary Field**: Fields only used sometimes\n- **Refused Bequest**: Subclass doesn't use parent methods\n\n### Change Preventers\n- **Divergent Change**: One class changes for many reasons\n- **Shotgun Surgery**: One change requires many small changes\n\n### Dispensables\n- **Comments**: Code that needs comments to be understood\n- **Duplicate Code**: Same structure in multiple places\n- **Dead Code**: Unused code\n- **Speculative Generality**: \"Maybe we'll need this\"\n\n### Couplers\n- **Feature Envy**: Method uses another class more than its own\n- **Inappropriate Intimacy**: Classes too tightly coupled\n- **Message Chains**: `a.b().c().d()`\n\n## Refactoring Techniques\n\n### Extract Method\n```python\n# Before\ndef print_invoice(order):\n    print(\"Invoice\")\n    print(f\"Customer: {order.customer}\")\n    total = 0\n    for item in order.items:\n        print(f\"{item.name}: ${item.price}\")\n        total += item.price\n    print(f\"Total: ${total}\")\n\n# After\ndef print_invoice(order):\n    print_header(order)\n    print_items(order)\n    print_total(order)\n\ndef print_header(order):\n    print(\"Invoice\")\n    print(f\"Customer: {order.customer}\")\n\ndef print_items(order):\n    for item in order.items:\n        print(f\"{item.name}: ${item.price}\")\n\ndef print_total(order):\n    total = calculate_total(order)\n    print(f\"Total: ${total}\")\n```\n\n### Replace Magic Number with Constant\n```javascript\n// Before\nif (age >= 18) { /* ... */ }\n\n// After\nconst LEGAL_AGE = 18;\nif (age >= LEGAL_AGE) { /* ... */ }\n```\n\n### Extract Class\n```python\n# Before\nclass Person:\n    def __init__(self, name, street, city, zipcode):\n        self.name = name\n        self.street = street\n        self.city = city\n        self.zipcode = zipcode\n\n# After\nclass Address:\n    def __init__(self, street, city, zipcode):\n        self.street = street\n        self.city = city\n        self.zipcode = zipcode\n\nclass Person:\n    def __init__(self, name, address):\n        self.name = name\n        self.address = address\n```\n\n### Replace Conditional with Polymorphism\n```python\n# Before\ndef get_speed(vehicle_type):\n    if vehicle_type == \"car\":\n        return 100\n    elif vehicle_type == \"bike\":\n        return 30\n    elif vehicle_type == \"plane\":\n        return 500\n\n# After\nclass Vehicle:\n    def get_speed(self):\n        raise NotImplementedError\n\nclass Car(Vehicle):\n    def get_speed(self):\n        return 100\n\nclass Bike(Vehicle):\n    def get_speed(self):\n        return 30\n\nclass Plane(Vehicle):\n    def get_speed(self):\n        return 500\n```\n\n### Introduce Parameter Object\n```java\n// Before\nvoid createOrder(String customerId, String productId, int quantity, double price) { }\n\n// After\nclass OrderDetails {\n    String customerId;\n    String productId;\n    int quantity;\n    double price;\n}\n\nvoid createOrder(OrderDetails details) { }\n```\n\n## Refactoring Priorities\n\n1. **Safety First**: Always have passing tests\n2. **Small Steps**: Commit after each successful refactoring\n3. **High ROI**: Focus on code that changes frequently\n4. **Team Agreement**: Ensure team agrees on changes\n\n## When NOT to Refactor\n\n- No tests exist (write tests first)\n- Close to deadline (schedule for later)\n- Code works and won't be changed\n- Rewrite would be faster\n\n## When to Use This Skill\n\nActivate when:\n- User asks to \"refactor\" code\n- User mentions \"clean up\" code\n- User wants to \"improve code quality\"\n- User asks to \"remove duplication\"\n- User mentions \"code smells\"",
      "trigger_keywords": [
        "refactor",
        "clean up code",
        "improve code",
        "code smell",
        "duplication",
        "extract method",
        "simplify"
      ],
      "allowed_tools": [
        "Read",
        "Write",
        "Edit",
        "Bash",
        "Grep",
        "Glob"
      ]
    },
    {
      "name": "api-design",
      "description": "Designs RESTful or GraphQL APIs following best practices including proper HTTP methods, status codes, versioning, pagination, error handling, and comprehensive documentation with OpenAPI/Swagger",
      "skill_template": "---\nname: api-design\ndescription: Designs RESTful or GraphQL APIs following best practices including proper HTTP methods, status codes, versioning, pagination, error handling, and comprehensive documentation with OpenAPI or Swagger specifications\nallowed-tools:\n  - Read\n  - Write\n  - Edit\n  - Grep\n  - Glob\n---\n\n# API Design Best Practices\n\nDesigns robust, consistent APIs following REST and GraphQL conventions.\n\n## RESTful API Principles\n\n### 1. Resource-Based URLs\n```\n✅ Good:\nGET    /api/v1/users\nGET    /api/v1/users/{id}\nPOST   /api/v1/users\nPUT    /api/v1/users/{id}\nDELETE /api/v1/users/{id}\nGET    /api/v1/users/{id}/orders\n\n❌ Bad:\nGET    /api/v1/getUsers\nPOST   /api/v1/createUser\nPOST   /api/v1/users/delete\n```\n\n### 2. HTTP Methods\n- **GET**: Retrieve resources (idempotent, cacheable)\n- **POST**: Create new resources\n- **PUT**: Update/replace entire resource (idempotent)\n- **PATCH**: Partial update\n- **DELETE**: Remove resource (idempotent)\n\n### 3. Status Codes\n- **200 OK**: Successful GET, PUT, PATCH, DELETE\n- **201 Created**: Successful POST\n- **204 No Content**: Successful DELETE with no response body\n- **400 Bad Request**: Invalid input\n- **401 Unauthorized**: Authentication required\n- **403 Forbidden**: Authenticated but not authorized\n- **404 Not Found**: Resource doesn't exist\n- **409 Conflict**: Resource conflict (e.g., duplicate)\n- **422 Unprocessable Entity**: Validation errors\n- **500 Internal Server Error**: Server error\n\n### 4. Request/Response Format\n\n```json\n// POST /api/v1/users\n// Request\n{\n  \"email\": \"user@example.com\",\n  \"name\": \"John Doe\",\n  \"role\": \"admin\"\n}\n\n// Response (201 Created)\n{\n  \"data\": {\n    \"id\": \"usr_1234567890\",\n    \"email\": \"user@example.com\",\n    \"name\": \"John Doe\",\n    \"role\": \"admin\",\n    \"createdAt\": \"2025-01-15T10:30:00Z\",\n    \"updatedAt\": \"2025-01-15T10:30:00Z\"\n  },\n  \"meta\": {\n    \"requestId\": \"req_abc123\"\n  }\n}\n```\n\n### 5. Error Responses\n\n```json\n// 422 Unprocessable Entity\n{\n  \"error\": {\n    \"code\": \"VALIDATION_ERROR\",\n    \"message\": \"Invalid input data\",\n    \"details\": [\n      {\n        \"field\": \"email\",\n        \"message\": \"Email is required\"\n      },\n      {\n        \"field\": \"name\",\n        \"message\": \"Name must be at least 2 characters\"\n      }\n    ]\n  },\n  \"meta\": {\n    \"requestId\": \"req_xyz789\"\n  }\n}\n```\n\n### 6. Pagination\n\n```\nGET /api/v1/users?page=2&limit=20&sort=-createdAt\n\nResponse:\n{\n  \"data\": [...],\n  \"pagination\": {\n    \"page\": 2,\n    \"limit\": 20,\n    \"totalPages\": 10,\n    \"totalItems\": 200,\n    \"hasNext\": true,\n    \"hasPrevious\": true\n  },\n  \"links\": {\n    \"self\": \"/api/v1/users?page=2&limit=20\",\n    \"first\": \"/api/v1/users?page=1&limit=20\",\n    \"prev\": \"/api/v1/users?page=1&limit=20\",\n    \"next\": \"/api/v1/users?page=3&limit=20\",\n    \"last\": \"/api/v1/users?page=10&limit=20\"\n  }\n}\n```\n\n### 7. Filtering and Searching\n\n```\nGET /api/v1/users?role=admin&status=active&search=john\nGET /api/v1/orders?startDate=2025-01-01&endDate=2025-01-31\nGET /api/v1/products?category=electronics&minPrice=100&maxPrice=500\n```\n\n### 8. Versioning\n\n```\n✅ URL Path: /api/v1/users, /api/v2/users\n✅ Header: Accept: application/vnd.myapi.v1+json\n❌ Query Param: /api/users?version=1 (less common)\n```\n\n### 9. Authentication\n\n```http\n# Bearer Token\nAuthorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...\n\n# API Key\nX-API-Key: your-api-key-here\n\n# Basic Auth (development only)\nAuthorization: Basic base64(username:password)\n```\n\n### 10. Rate Limiting\n\n```http\nX-RateLimit-Limit: 1000\nX-RateLimit-Remaining: 999\nX-RateLimit-Reset: 1640000000\n\n# 429 Too Many Requests\n{\n  \"error\": {\n    \"code\": \"RATE_LIMIT_EXCEEDED\",\n    \"message\": \"Too many requests. Please try again later.\",\n    \"retryAfter\": 3600\n  }\n}\n```\n\n## OpenAPI/Swagger Example\n\n```yaml\nopenapi: 3.0.0\ninfo:\n  title: User API\n  version: 1.0.0\n  description: API for managing users\n\npaths:\n  /users:\n    get:\n      summary: List all users\n      parameters:\n        - name: page\n          in: query\n          schema:\n            type: integer\n            default: 1\n        - name: limit\n          in: query\n          schema:\n            type: integer\n            default: 20\n      responses:\n        '200':\n          description: Successful response\n          content:\n            application/json:\n              schema:\n                type: object\n                properties:\n                  data:\n                    type: array\n                    items:\n                      $ref: '#/components/schemas/User'\n    post:\n      summary: Create a new user\n      requestBody:\n        required: true\n        content:\n          application/json:\n            schema:\n              $ref: '#/components/schemas/CreateUser'\n      responses:\n        '201':\n          description: User created\n          content:\n            application/json:\n              schema:\n                $ref: '#/components/schemas/User'\n\ncomponents:\n  schemas:\n    User:\n      type: object\n      properties:\n        id:\n          type: string\n        email:\n          type: string\n        name:\n          type: string\n        createdAt:\n          type: string\n          format: date-time\n```\n\n## When to Use This Skill\n\nActivate when:\n- User asks to \"design an API\"\n- User mentions \"REST API\" or \"GraphQL\"\n- User wants to \"create endpoints\"\n- User asks about \"API best practices\"\n- User needs \"API documentation\"",
      "trigger_keywords": [
        "API design",
        "REST API",
        "GraphQL",
        "endpoints",
        "API documentation",
        "OpenAPI",
        "Swagger",
        "HTTP methods"
      ],
      "allowed_tools": [
        "Read",
        "Write",
        "Edit",
        "Grep",
        "Glob"
      ]
    },
    {
      "name": "database-schema",
      "description": "Designs normalized database schemas with proper relationships, indexes, constraints, and migrations, following best practices for PostgreSQL, MySQL, or MongoDB",
      "skill_template": "---\nname: database-schema\ndescription: Designs normalized database schemas with proper relationships, indexes, constraints, and migrations, following best practices for PostgreSQL, MySQL, or MongoDB\nallowed-tools:\n  - Read\n  - Write\n  - Edit\n  - Bash\n  - Grep\n---\n\n# Database Schema Design\n\nDesigns efficient, normalized database schemas with proper relationships and constraints.\n\n## Design Principles\n\n### 1. Normalization\n\n**First Normal Form (1NF)**:\n- Atomic values (no arrays or JSON in columns)\n- Unique column names\n- Order doesn't matter\n\n**Second Normal Form (2NF)**:\n- Must be in 1NF\n- No partial dependencies on composite keys\n\n**Third Normal Form (3NF)**:\n- Must be in 2NF\n- No transitive dependencies\n\n### 2. Naming Conventions\n\n```sql\n-- Tables: plural, snake_case\nusers, blog_posts, order_items\n\n-- Columns: singular, snake_case\nuser_id, created_at, first_name\n\n-- Primary keys: id or table_id\nid, user_id\n\n-- Foreign keys: singular_table_id\nuser_id, blog_post_id\n\n-- Indexes: idx_table_column\nidx_users_email, idx_posts_user_id_created_at\n\n-- Constraints: table_column_constraint\nusers_email_unique, orders_status_check\n```\n\n### 3. Data Types\n\n```sql\n-- PostgreSQL Best Practices\n\n-- IDs: Use BIGSERIAL or UUID\nid BIGSERIAL PRIMARY KEY\nid UUID PRIMARY KEY DEFAULT gen_random_uuid()\n\n-- Strings\nVARCHAR(255)  -- For variable-length with limit\nTEXT          -- For unlimited text\n\n-- Numbers\nINTEGER       -- -2B to 2B\nBIGINT        -- Large integers\nNUMERIC(10,2) -- Exact decimals (money)\n\n-- Dates/Times\nTIMESTAMP WITH TIME ZONE  -- Always use timezone\nDATE                       -- Date only\n\n-- Boolean\nBOOLEAN  -- true/false, not 0/1\n\n-- JSON\nJSONB    -- Binary JSON (faster, indexable)\n```\n\n## Example Schema: E-commerce\n\n```sql\n-- Users table\nCREATE TABLE users (\n  id BIGSERIAL PRIMARY KEY,\n  email VARCHAR(255) UNIQUE NOT NULL,\n  password_hash VARCHAR(255) NOT NULL,\n  first_name VARCHAR(100) NOT NULL,\n  last_name VARCHAR(100) NOT NULL,\n  created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,\n  updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,\n  deleted_at TIMESTAMP WITH TIME ZONE,\n  \n  CONSTRAINT users_email_check CHECK (email ~* '^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,}$')\n);\n\nCREATE INDEX idx_users_email ON users(email);\nCREATE INDEX idx_users_deleted_at ON users(deleted_at) WHERE deleted_at IS NULL;\n\n-- Products table\nCREATE TABLE products (\n  id BIGSERIAL PRIMARY KEY,\n  sku VARCHAR(50) UNIQUE NOT NULL,\n  name VARCHAR(255) NOT NULL,\n  description TEXT,\n  price NUMERIC(10,2) NOT NULL,\n  stock_quantity INTEGER NOT NULL DEFAULT 0,\n  category_id BIGINT REFERENCES categories(id),\n  created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,\n  updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,\n  \n  CONSTRAINT products_price_positive CHECK (price >= 0),\n  CONSTRAINT products_stock_non_negative CHECK (stock_quantity >= 0)\n);\n\nCREATE INDEX idx_products_category_id ON products(category_id);\nCREATE INDEX idx_products_sku ON products(sku);\n\n-- Orders table\nCREATE TABLE orders (\n  id BIGSERIAL PRIMARY KEY,\n  order_number VARCHAR(50) UNIQUE NOT NULL,\n  user_id BIGINT NOT NULL REFERENCES users(id),\n  status VARCHAR(20) NOT NULL DEFAULT 'pending',\n  total_amount NUMERIC(10,2) NOT NULL,\n  created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,\n  updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,\n  \n  CONSTRAINT orders_status_check CHECK (status IN ('pending', 'processing', 'shipped', 'delivered', 'cancelled')),\n  CONSTRAINT orders_total_positive CHECK (total_amount >= 0)\n);\n\nCREATE INDEX idx_orders_user_id ON orders(user_id);\nCREATE INDEX idx_orders_status ON orders(status);\nCREATE INDEX idx_orders_created_at ON orders(created_at DESC);\n\n-- Order items (junction table)\nCREATE TABLE order_items (\n  id BIGSERIAL PRIMARY KEY,\n  order_id BIGINT NOT NULL REFERENCES orders(id) ON DELETE CASCADE,\n  product_id BIGINT NOT NULL REFERENCES products(id),\n  quantity INTEGER NOT NULL,\n  unit_price NUMERIC(10,2) NOT NULL,\n  subtotal NUMERIC(10,2) NOT NULL,\n  \n  CONSTRAINT order_items_quantity_positive CHECK (quantity > 0),\n  CONSTRAINT order_items_unit_price_positive CHECK (unit_price >= 0),\n  UNIQUE(order_id, product_id)\n);\n\nCREATE INDEX idx_order_items_order_id ON order_items(order_id);\nCREATE INDEX idx_order_items_product_id ON order_items(product_id);\n```\n\n## Relationships\n\n### One-to-Many\n```sql\n-- One user has many orders\nusers (1) ----< orders (many)\n\nCREATE TABLE orders (\n  user_id BIGINT NOT NULL REFERENCES users(id)\n);\n```\n\n### Many-to-Many\n```sql\n-- Many products in many categories\nproducts (many) >----< categories (many)\n\n-- Junction table\nCREATE TABLE product_categories (\n  product_id BIGINT REFERENCES products(id),\n  category_id BIGINT REFERENCES categories(id),\n  PRIMARY KEY (product_id, category_id)\n);\n```\n\n### One-to-One\n```sql\n-- One user has one profile\nusers (1) ---- profiles (1)\n\nCREATE TABLE profiles (\n  user_id BIGINT PRIMARY KEY REFERENCES users(id),\n  bio TEXT,\n  avatar_url VARCHAR(500)\n);\n```\n\n## Indexes\n\n```sql\n-- Single column index\nCREATE INDEX idx_users_email ON users(email);\n\n-- Composite index (order matters!)\nCREATE INDEX idx_orders_user_status ON orders(user_id, status);\n\n-- Partial index (for soft deletes)\nCREATE INDEX idx_users_active ON users(id) WHERE deleted_at IS NULL;\n\n-- Unique index\nCREATE UNIQUE INDEX idx_users_email_unique ON users(email);\n\n-- Full-text search (PostgreSQL)\nCREATE INDEX idx_products_search ON products USING GIN(to_tsvector('english', name || ' ' || description));\n```\n\n## Migrations\n\n```sql\n-- Migration: 001_create_users.sql\nBEGIN;\n\nCREATE TABLE users (\n  id BIGSERIAL PRIMARY KEY,\n  email VARCHAR(255) UNIQUE NOT NULL,\n  created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP\n);\n\nCOMMIT;\n\n-- Migration: 002_add_user_names.sql\nBEGIN;\n\nALTER TABLE users\n  ADD COLUMN first_name VARCHAR(100),\n  ADD COLUMN last_name VARCHAR(100);\n\nUPDATE users SET first_name = '', last_name = '';\n\nALTER TABLE users\n  ALTER COLUMN first_name SET NOT NULL,\n  ALTER COLUMN last_name SET NOT NULL;\n\nCOMMIT;\n```\n\n## When to Use This Skill\n\nActivate when:\n- User asks to \"design a database\"\n- User mentions \"schema design\"\n- User needs \"database migrations\"\n- User asks about \"table relationships\"\n- User wants to \"create tables\"",
      "trigger_keywords": [
        "database schema",
        "database design",
        "table design",
        "migrations",
        "SQL",
        "PostgreSQL",
        "MySQL",
        "relationships"
      ],
      "allowed_tools": [
        "Read",
        "Write",
        "Edit",
        "Bash",
        "Grep"
      ]
    },
    {
      "name": "security-audit",
      "description": "Performs security audit checking for common vulnerabilities including SQL injection, XSS, CSRF, authentication issues, sensitive data exposure, and provides OWASP Top 10 compliance recommendations",
      "skill_template": "---\nname: security-audit\ndescription: Performs security audit checking for common vulnerabilities including SQL injection, XSS, CSRF, authentication issues, sensitive data exposure, and provides OWASP Top 10 compliance recommendations\nallowed-tools:\n  - Read\n  - Grep\n  - Glob\n  - Bash\n---\n\n# Security Audit\n\nPerforms comprehensive security review based on OWASP Top 10 and industry best practices.\n\n## OWASP Top 10 (2021)\n\n### 1. Broken Access Control\n\n**What to Check**:\n- Users can access resources they shouldn't\n- Missing authorization checks\n- Insecure direct object references (IDOR)\n- Path traversal vulnerabilities\n\n**Example Vulnerability**:\n```python\n# BAD: No authorization check\n@app.route('/user/<user_id>')\ndef get_user(user_id):\n    user = User.query.get(user_id)\n    return jsonify(user.to_dict())\n\n# GOOD: Authorization check\n@app.route('/user/<user_id>')\n@login_required\ndef get_user(user_id):\n    if current_user.id != user_id and not current_user.is_admin:\n        abort(403)\n    user = User.query.get(user_id)\n    return jsonify(user.to_dict())\n```\n\n### 2. Cryptographic Failures\n\n**What to Check**:\n- Passwords stored in plaintext or weak hashing\n- Sensitive data transmitted over HTTP\n- Weak encryption algorithms\n- Hardcoded secrets\n\n**Example Fix**:\n```python\n# BAD: Plaintext password\npassword = request.form['password']\nuser.password = password\n\n# GOOD: Hashed password\nimport bcrypt\npassword = request.form['password']\nuser.password_hash = bcrypt.hashpw(password.encode(), bcrypt.gensalt())\n\n# BAD: Hardcoded secret\nSECRET_KEY = \"my-secret-key-123\"\n\n# GOOD: Environment variable\nimport os\nSECRET_KEY = os.environ.get('SECRET_KEY')\nif not SECRET_KEY:\n    raise ValueError(\"SECRET_KEY environment variable must be set\")\n```\n\n### 3. Injection\n\n**What to Check**:\n- SQL injection\n- NoSQL injection\n- Command injection\n- LDAP injection\n\n**Example Vulnerabilities**:\n```python\n# SQL INJECTION - BAD\nquery = f\"SELECT * FROM users WHERE email = '{email}'\"\ndb.execute(query)\n\n# SQL INJECTION - GOOD\nquery = \"SELECT * FROM users WHERE email = ?\"\ndb.execute(query, (email,))\n\n# COMMAND INJECTION - BAD\nos.system(f\"ping {user_input}\")\n\n# COMMAND INJECTION - GOOD\nimport subprocess\nsubprocess.run([\"ping\", \"-c\", \"1\", user_input], check=True)\n```\n\n### 4. Insecure Design\n\n**What to Check**:\n- Missing rate limiting\n- No account lockout after failed logins\n- Weak password policies\n- Missing security headers\n\n**Example Fixes**:\n```python\n# Rate limiting\nfrom flask_limiter import Limiter\n\nlimiter = Limiter(app, key_func=get_remote_address)\n\n@app.route('/api/login', methods=['POST'])\n@limiter.limit(\"5 per minute\")\ndef login():\n    # Login logic\n    pass\n\n# Security headers\nfrom flask_talisman import Talisman\n\nTalisman(app, \n    force_https=True,\n    strict_transport_security=True,\n    content_security_policy={\n        'default-src': \"'self'\",\n        'script-src': \"'self' 'unsafe-inline'\"\n    }\n)\n```\n\n### 5. Security Misconfiguration\n\n**What to Check**:\n- Default credentials still enabled\n- Unnecessary features enabled\n- Stack traces exposed to users\n- Directory listing enabled\n- Verbose error messages\n\n**Example Fixes**:\n```python\n# BAD: Debug mode in production\napp.run(debug=True)\n\n# GOOD: Debug only in development\napp.run(debug=os.environ.get('ENV') == 'development')\n\n# BAD: Detailed error to user\n@app.errorhandler(500)\ndef error_handler(error):\n    return str(error), 500\n\n# GOOD: Generic error to user, log details\n@app.errorhandler(500)\ndef error_handler(error):\n    logger.error(f\"Internal error: {error}\", exc_info=True)\n    return {\"error\": \"Internal server error\"}, 500\n```\n\n### 6. Vulnerable and Outdated Components\n\n**What to Check**:\n- Outdated dependencies\n- Known vulnerabilities in packages\n- Unused dependencies\n\n**Commands to Run**:\n```bash\n# Python\npip install safety\nsafety check\npip list --outdated\n\n# Node.js\nnpm audit\nnpm outdated\n\n# Ruby\nbundle audit check --update\n\n# Go\ngo list -m -u all\n```\n\n### 7. Identification and Authentication Failures\n\n**What to Check**:\n- Weak password requirements\n- No multi-factor authentication\n- Session fixation vulnerabilities\n- Predictable session tokens\n\n**Example Fixes**:\n```python\n# Password validation\nimport re\n\ndef validate_password(password):\n    if len(password) < 12:\n        return False, \"Password must be at least 12 characters\"\n    if not re.search(r'[A-Z]', password):\n        return False, \"Password must contain uppercase letter\"\n    if not re.search(r'[a-z]', password):\n        return False, \"Password must contain lowercase letter\"\n    if not re.search(r'[0-9]', password):\n        return False, \"Password must contain number\"\n    if not re.search(r'[!@#$%^&*]', password):\n        return False, \"Password must contain special character\"\n    return True, \"Password is valid\"\n\n# Session security\napp.config['SESSION_COOKIE_SECURE'] = True  # HTTPS only\napp.config['SESSION_COOKIE_HTTPONLY'] = True  # No JS access\napp.config['SESSION_COOKIE_SAMESITE'] = 'Lax'  # CSRF protection\napp.config['PERMANENT_SESSION_LIFETIME'] = timedelta(hours=1)\n```\n\n### 8. Software and Data Integrity Failures\n\n**What to Check**:\n- Unsigned software updates\n- Insecure deserialization\n- CI/CD pipeline security\n\n**Example Vulnerability**:\n```python\n# BAD: Insecure deserialization\nimport pickle\ndata = pickle.loads(user_input)  # Can execute arbitrary code!\n\n# GOOD: Use safe serialization\nimport json\ndata = json.loads(user_input)\n```\n\n### 9. Security Logging and Monitoring Failures\n\n**What to Check**:\n- No logging of security events\n- Logs not monitored\n- Insufficient log detail\n\n**Example Implementation**:\n```python\nimport logging\n\n# Security event logging\nsecurity_logger = logging.getLogger('security')\n\n@app.route('/login', methods=['POST'])\ndef login():\n    username = request.form['username']\n    password = request.form['password']\n    \n    user = authenticate(username, password)\n    if user:\n        security_logger.info(f\"Successful login: {username} from {request.remote_addr}\")\n        return {\"success\": True}\n    else:\n        security_logger.warning(f\"Failed login attempt: {username} from {request.remote_addr}\")\n        return {\"error\": \"Invalid credentials\"}, 401\n```\n\n### 10. Server-Side Request Forgery (SSRF)\n\n**What to Check**:\n- User-provided URLs fetched without validation\n- Internal network accessible from user input\n\n**Example Fix**:\n```python\nfrom urllib.parse import urlparse\nimport ipaddress\n\ndef is_safe_url(url):\n    parsed = urlparse(url)\n    \n    # Only allow HTTP/HTTPS\n    if parsed.scheme not in ['http', 'https']:\n        return False\n    \n    # Block private IPs\n    try:\n        ip = ipaddress.ip_address(parsed.hostname)\n        if ip.is_private or ip.is_loopback:\n            return False\n    except ValueError:\n        pass  # Not an IP, continue\n    \n    # Whitelist domains\n    allowed_domains = ['api.example.com', 'cdn.example.com']\n    if parsed.hostname not in allowed_domains:\n        return False\n    \n    return True\n\n# Usage\nuser_url = request.form['url']\nif not is_safe_url(user_url):\n    abort(400, \"Invalid URL\")\n```\n\n## Security Checklist\n\n- [ ] All user inputs validated and sanitized\n- [ ] Parameterized queries (no string concatenation)\n- [ ] Passwords hashed with bcrypt/argon2\n- [ ] Secrets in environment variables, not code\n- [ ] HTTPS enforced\n- [ ] Security headers configured (CSP, HSTS, X-Frame-Options)\n- [ ] Rate limiting on authentication endpoints\n- [ ] CSRF protection enabled\n- [ ] Session cookies secure, httpOnly, sameSite\n- [ ] Dependencies up to date\n- [ ] Error messages don't leak sensitive info\n- [ ] Security events logged\n- [ ] Authorization checks on all endpoints\n- [ ] File uploads validated (type, size, content)\n- [ ] Output encoding for XSS prevention\n\n## When to Use This Skill\n\nActivate when:\n- User asks for \"security audit\"\n- User mentions \"vulnerabilities\"\n- User asks to \"check security\"\n- User mentions \"OWASP\"\n- User asks about \"security best practices\"",
      "trigger_keywords": [
        "security audit",
        "security review",
        "vulnerabilities",
        "OWASP",
        "security check",
        "penetration test",
        "security best practices"
      ],
      "allowed_tools": [
        "Read",
        "Grep",
        "Glob",
        "Bash"
      ]
    },
    {
      "name": "documentation-generator",
      "description": "Generates comprehensive documentation including README files, API docs, code comments, JSDoc/Docstrings, changelog, and architecture diagrams using industry-standard formats",
      "skill_template": "---\nname: documentation-generator\ndescription: Generates comprehensive documentation including README files, API docs, code comments, JSDoc or Python docstrings, changelog, and architecture diagrams using industry-standard formats\nallowed-tools:\n  - Read\n  - Write\n  - Edit\n  - Grep\n  - Glob\n  - Bash\n---\n\n# Documentation Generator\n\nGenerates clear, comprehensive documentation following industry best practices.\n\n## README.md Structure\n\n```markdown\n# Project Name\n\nBrief description of what the project does and who it's for.\n\n## Features\n\n- Feature 1\n- Feature 2\n- Feature 3\n\n## Installation\n\n```bash\n# Clone the repository\ngit clone https://github.com/username/project.git\ncd project\n\n# Install dependencies\nnpm install\n# or\npip install -r requirements.txt\n```\n\n## Quick Start\n\n```python\nfrom myproject import MyClass\n\n# Basic usage example\nobj = MyClass()\nresult = obj.do_something()\nprint(result)\n```\n\n## Configuration\n\nCreate a `.env` file:\n\n```env\nDATABASE_URL=postgresql://localhost/mydb\nAPI_KEY=your-api-key\nENVIRONMENT=development\n```\n\n## Usage\n\n### Basic Example\n\n```python\n# Detailed usage example\n```\n\n### Advanced Example\n\n```python\n# Advanced usage example\n```\n\n## API Documentation\n\nSee [API.md](./API.md) for detailed API documentation.\n\n## Development\n\n```bash\n# Run tests\nnpm test\n\n# Run linter\nnpm run lint\n\n# Run in development mode\nnpm run dev\n```\n\n## Project Structure\n\n```\nproject/\n├── src/\n│   ├── components/\n│   ├── services/\n│   └── utils/\n├── tests/\n├── docs/\n├── package.json\n└── README.md\n```\n\n## Contributing\n\nSee [CONTRIBUTING.md](./CONTRIBUTING.md) for guidelines.\n\n## License\n\nMIT License - see [LICENSE](./LICENSE) for details.\n\n## Support\n\n- Documentation: https://docs.example.com\n- Issues: https://github.com/username/project/issues\n- Email: support@example.com\n```\n\n## Code Comments Best Practices\n\n### Python Docstrings\n\n```python\ndef calculate_total(items: list[dict], tax_rate: float = 0.0) -> float:\n    \"\"\"\n    Calculate the total cost of items including tax.\n    \n    This function sums the price of all items and applies the specified\n    tax rate to calculate the final total.\n    \n    Args:\n        items: List of item dictionaries with 'price' and 'quantity' keys.\n            Example: [{'price': 10.00, 'quantity': 2}]\n        tax_rate: Tax rate as a decimal (e.g., 0.08 for 8%). Defaults to 0.0.\n    \n    Returns:\n        Total cost as a float, rounded to 2 decimal places.\n    \n    Raises:\n        ValueError: If tax_rate is negative or items is empty.\n        KeyError: If an item is missing 'price' or 'quantity' keys.\n    \n    Examples:\n        >>> items = [{'price': 10.00, 'quantity': 2}, {'price': 5.00, 'quantity': 1}]\n        >>> calculate_total(items, tax_rate=0.08)\n        27.0\n        \n        >>> calculate_total([], tax_rate=0.08)\n        Traceback (most recent call last):\n        ValueError: items list cannot be empty\n    \"\"\"\n    if not items:\n        raise ValueError(\"items list cannot be empty\")\n    if tax_rate < 0:\n        raise ValueError(\"tax_rate cannot be negative\")\n    \n    subtotal = sum(item['price'] * item['quantity'] for item in items)\n    total = subtotal * (1 + tax_rate)\n    return round(total, 2)\n```\n\n### JavaScript JSDoc\n\n```javascript\n/**\n * Calculate the total cost of items including tax.\n * \n * @param {Array<{price: number, quantity: number}>} items - Array of items to calculate total for\n * @param {number} [taxRate=0] - Tax rate as decimal (e.g., 0.08 for 8%)\n * @returns {number} Total cost rounded to 2 decimal places\n * @throws {Error} If items array is empty\n * @throws {Error} If taxRate is negative\n * \n * @example\n * const items = [{price: 10.00, quantity: 2}, {price: 5.00, quantity: 1}];\n * const total = calculateTotal(items, 0.08);\n * console.log(total); // 27.0\n */\nfunction calculateTotal(items, taxRate = 0) {\n  if (items.length === 0) {\n    throw new Error('items array cannot be empty');\n  }\n  if (taxRate < 0) {\n    throw new Error('taxRate cannot be negative');\n  }\n  \n  const subtotal = items.reduce((sum, item) => sum + item.price * item.quantity, 0);\n  const total = subtotal * (1 + taxRate);\n  return Math.round(total * 100) / 100;\n}\n```\n\n### Go Documentation\n\n```go\n// CalculateTotal calculates the total cost of items including tax.\n//\n// This function sums the price of all items and applies the specified\n// tax rate to calculate the final total.\n//\n// Parameters:\n//   - items: Slice of Item structs with Price and Quantity fields\n//   - taxRate: Tax rate as a decimal (e.g., 0.08 for 8%)\n//\n// Returns:\n//   - Total cost as float64, rounded to 2 decimal places\n//\n// Example:\n//\n//\titems := []Item{{Price: 10.00, Quantity: 2}, {Price: 5.00, Quantity: 1}}\n//\ttotal := CalculateTotal(items, 0.08)\n//\tfmt.Println(total) // 27.0\nfunc CalculateTotal(items []Item, taxRate float64) (float64, error) {\n    if len(items) == 0 {\n        return 0, fmt.Errorf(\"items slice cannot be empty\")\n    }\n    if taxRate < 0 {\n        return 0, fmt.Errorf(\"taxRate cannot be negative\")\n    }\n    \n    var subtotal float64\n    for _, item := range items {\n        subtotal += item.Price * float64(item.Quantity)\n    }\n    total := subtotal * (1 + taxRate)\n    return math.Round(total*100) / 100, nil\n}\n```\n\n## API Documentation\n\n```markdown\n# API Documentation\n\n## Authentication\n\nAll API requests require authentication using a Bearer token:\n\n```http\nAuthorization: Bearer YOUR_API_TOKEN\n```\n\n## Endpoints\n\n### List Users\n\n**GET** `/api/v1/users`\n\nReturns a paginated list of users.\n\n**Query Parameters:**\n\n| Parameter | Type | Required | Description |\n|-----------|------|----------|-------------|\n| page | integer | No | Page number (default: 1) |\n| limit | integer | No | Items per page (default: 20, max: 100) |\n| role | string | No | Filter by role (admin, user) |\n| search | string | No | Search by name or email |\n\n**Response:**\n\n```json\n{\n  \"data\": [\n    {\n      \"id\": \"usr_1234567890\",\n      \"email\": \"user@example.com\",\n      \"name\": \"John Doe\",\n      \"role\": \"admin\",\n      \"createdAt\": \"2025-01-15T10:30:00Z\"\n    }\n  ],\n  \"pagination\": {\n    \"page\": 1,\n    \"limit\": 20,\n    \"totalPages\": 5,\n    \"totalItems\": 100\n  }\n}\n```\n\n**Error Responses:**\n\n- `401 Unauthorized`: Invalid or missing authentication token\n- `403 Forbidden`: Insufficient permissions\n- `422 Unprocessable Entity`: Invalid query parameters\n\n```json\n{\n  \"error\": {\n    \"code\": \"VALIDATION_ERROR\",\n    \"message\": \"Invalid query parameters\",\n    \"details\": [\n      {\n        \"field\": \"limit\",\n        \"message\": \"Must be between 1 and 100\"\n      }\n    ]\n  }\n}\n```\n```\n\n## CHANGELOG.md\n\n```markdown\n# Changelog\n\nAll notable changes to this project will be documented in this file.\n\nThe format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),\nand this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).\n\n## [Unreleased]\n\n### Added\n- New feature in development\n\n## [2.1.0] - 2025-01-15\n\n### Added\n- OAuth2 authentication support (#234)\n- Rate limiting on all API endpoints (#245)\n- Pagination support for list endpoints (#256)\n\n### Changed\n- Improved error messages for validation failures (#267)\n- Updated dependencies to latest versions (#278)\n\n### Fixed\n- Race condition in user creation (#289)\n- Memory leak in background worker (#290)\n\n### Security\n- Fixed SQL injection vulnerability in search endpoint (CVE-2025-1234)\n\n## [2.0.0] - 2025-01-01\n\n### Added\n- Complete API redesign with v2 endpoints\n- GraphQL support\n\n### Changed\n- **BREAKING**: Changed authentication from API keys to JWT tokens\n- **BREAKING**: Renamed `/users` to `/api/v2/users`\n\n### Removed\n- **BREAKING**: Removed deprecated v1 endpoints\n\n### Deprecated\n- Legacy authentication method (will be removed in 3.0.0)\n\n## [1.0.0] - 2024-12-01\n\n### Added\n- Initial release\n- User management API\n- Basic authentication\n```\n\n## When to Use This Skill\n\nActivate when:\n- User asks to \"write documentation\"\n- User mentions \"README\"\n- User needs \"API documentation\"\n- User asks for \"comments\" or \"docstrings\"\n- User wants to \"document the code\"",
      "trigger_keywords": [
        "documentation",
        "README",
        "API docs",
        "docstrings",
        "comments",
        "changelog",
        "document code"
      ],
      "allowed_tools": [
        "Read",
        "Write",
        "Edit",
        "Grep",
        "Glob",
        "Bash"
      ]
    },
    {
      "name": "performance-optimization",
      "description": "Analyzes and optimizes code performance by profiling bottlenecks, implementing caching strategies, optimizing database queries, reducing algorithmic complexity, and measuring improvements",
      "skill_template": "---\nname: performance-optimization\ndescription: Analyzes and optimizes code performance by profiling bottlenecks, implementing caching strategies, optimizing database queries, reducing algorithmic complexity, and measuring improvements with benchmarks\nallowed-tools:\n  - Read\n  - Write\n  - Edit\n  - Bash\n  - Grep\n  - Glob\n---\n\n# Performance Optimization\n\nSystematically identifies and resolves performance bottlenecks.\n\n## Optimization Process\n\n1. **Measure First**: Profile before optimizing\n2. **Identify Bottlenecks**: Find the slowest 20% causing 80% of problems\n3. **Optimize**: Apply targeted improvements\n4. **Measure Again**: Verify improvements\n5. **Iterate**: Repeat for next bottleneck\n\n## Profiling Tools\n\n### Python\n```python\n# cProfile - Function-level profiling\nimport cProfile\nimport pstats\n\nprofiler = cProfile.Profile()\nprofiler.enable()\n\n# Your code here\nresult = expensive_function()\n\nprofiler.disable()\nstats = pstats.Stats(profiler)\nstats.sort_stats('cumulative')\nstats.print_stats(20)  # Top 20 functions\n\n# line_profiler - Line-level profiling\n# Install: pip install line_profiler\n# Usage: kernprof -l -v script.py\n\n@profile\ndef expensive_function():\n    # Each line will be profiled\n    pass\n\n# memory_profiler - Memory usage\n# Install: pip install memory_profiler\nfrom memory_profiler import profile\n\n@profile\ndef memory_intensive():\n    large_list = [i for i in range(1000000)]\n    return sum(large_list)\n```\n\n### JavaScript/Node.js\n```javascript\n// Built-in profiler\nconst { performance } = require('perf_hooks');\n\nconst start = performance.now();\nexpensiveOperation();\nconst end = performance.now();\nconsole.log(`Took ${end - start}ms`);\n\n// Chrome DevTools\n// Run: node --inspect script.js\n// Then open chrome://inspect\n\n// Clinic.js - Comprehensive profiling\n// Install: npm install -g clinic\n// Usage: clinic doctor -- node script.js\n```\n\n### Go\n```go\nimport (\n    \"runtime/pprof\"\n    \"os\"\n)\n\n// CPU profiling\nf, _ := os.Create(\"cpu.prof\")\npprof.StartCPUProfile(f)\ndefer pprof.StopCPUProfile()\n\n// Your code here\n\n// Analyze: go tool pprof cpu.prof\n\n// Memory profiling\nf, _ := os.Create(\"mem.prof\")\npprof.WriteHeapProfile(f)\nf.Close()\n\n// Analyze: go tool pprof mem.prof\n```\n\n## Common Optimizations\n\n### 1. Algorithm Complexity\n\n```python\n# BAD: O(n²) - Nested loops\ndef find_duplicates(items):\n    duplicates = []\n    for i in range(len(items)):\n        for j in range(i + 1, len(items)):\n            if items[i] == items[j]:\n                duplicates.append(items[i])\n    return duplicates\n\n# GOOD: O(n) - Using set\ndef find_duplicates(items):\n    seen = set()\n    duplicates = set()\n    for item in items:\n        if item in seen:\n            duplicates.add(item)\n        seen.add(item)\n    return list(duplicates)\n```\n\n### 2. Database Query Optimization\n\n```python\n# BAD: N+1 queries\nusers = User.objects.all()  # 1 query\nfor user in users:\n    print(user.profile.bio)  # N queries!\n\n# GOOD: Single query with join\nusers = User.objects.select_related('profile').all()  # 1 query\nfor user in users:\n    print(user.profile.bio)\n\n# BAD: Loading all data\nexpensive_users = User.objects.filter(total_spent__gt=10000).all()\ncount = len(expensive_users)  # Loads all users into memory\n\n# GOOD: Count in database\ncount = User.objects.filter(total_spent__gt=10000).count()\n\n# BAD: Missing index\n# SELECT * FROM users WHERE email = 'user@example.com'  -- Slow!\n\n# GOOD: Add index\n# CREATE INDEX idx_users_email ON users(email);  -- Fast!\n```\n\n### 3. Caching\n\n```python\n# In-memory cache\nfrom functools import lru_cache\n\n@lru_cache(maxsize=1000)\ndef expensive_calculation(n):\n    # Results cached automatically\n    return sum(i**2 for i in range(n))\n\n# Redis cache\nimport redis\nimport json\n\ncache = redis.Redis(host='localhost', port=6379, db=0)\n\ndef get_user(user_id):\n    # Try cache first\n    cached = cache.get(f'user:{user_id}')\n    if cached:\n        return json.loads(cached)\n    \n    # Cache miss - fetch from database\n    user = db.query(User).get(user_id)\n    cache.setex(f'user:{user_id}', 3600, json.dumps(user.to_dict()))\n    return user\n\n# HTTP caching\nfrom flask import Flask, make_response\n\n@app.route('/api/public-data')\ndef public_data():\n    response = make_response(get_data())\n    response.headers['Cache-Control'] = 'public, max-age=3600'\n    response.headers['ETag'] = generate_etag(data)\n    return response\n```\n\n### 4. Lazy Loading\n\n```python\n# BAD: Load everything upfront\nclass Report:\n    def __init__(self, report_id):\n        self.data = self.fetch_all_data(report_id)  # Slow!\n        self.charts = self.generate_charts(self.data)  # Slow!\n        self.pdf = self.create_pdf(self.data)  # Slow!\n\n# GOOD: Load on-demand\nclass Report:\n    def __init__(self, report_id):\n        self.report_id = report_id\n        self._data = None\n        self._charts = None\n        self._pdf = None\n    \n    @property\n    def data(self):\n        if self._data is None:\n            self._data = self.fetch_all_data(self.report_id)\n        return self._data\n    \n    @property\n    def charts(self):\n        if self._charts is None:\n            self._charts = self.generate_charts(self.data)\n        return self._charts\n```\n\n### 5. Batch Operations\n\n```python\n# BAD: Individual operations\nfor user_id in user_ids:\n    user = db.query(User).get(user_id)  # N queries\n    user.send_email()\n\n# GOOD: Batch query\nusers = db.query(User).filter(User.id.in_(user_ids)).all()  # 1 query\nfor user in users:\n    user.send_email()\n\n# BAD: Individual inserts\nfor item in items:\n    db.add(Item(**item))  # N inserts\n    db.commit()\n\n# GOOD: Bulk insert\ndb.bulk_insert_mappings(Item, items)  # 1 insert\ndb.commit()\n```\n\n### 6. Async/Concurrent Operations\n\n```python\n# BAD: Sequential I/O\nimport requests\n\nresults = []\nfor url in urls:\n    response = requests.get(url)  # Blocking!\n    results.append(response.json())\n\n# GOOD: Concurrent I/O\nimport asyncio\nimport aiohttp\n\nasync def fetch(session, url):\n    async with session.get(url) as response:\n        return await response.json()\n\nasync def fetch_all(urls):\n    async with aiohttp.ClientSession() as session:\n        tasks = [fetch(session, url) for url in urls]\n        return await asyncio.gather(*tasks)\n\nresults = asyncio.run(fetch_all(urls))\n```\n\n### 7. Memory Optimization\n\n```python\n# BAD: Loading entire file\nwith open('large_file.txt') as f:\n    data = f.read()  # Loads entire file into memory\n    for line in data.split('\\n'):\n        process(line)\n\n# GOOD: Streaming\nwith open('large_file.txt') as f:\n    for line in f:  # One line at a time\n        process(line)\n\n# BAD: Storing all results\nresults = [expensive_calculation(i) for i in range(1000000)]  # Huge list\n\n# GOOD: Generator\nresults = (expensive_calculation(i) for i in range(1000000))  # Lazy\nfor result in results:\n    if result > threshold:\n        break  # Can stop early\n```\n\n## Performance Benchmarking\n\n```python\nimport timeit\n\n# Measure execution time\ndef benchmark():\n    setup = \"from __main__ import my_function\"\n    stmt = \"my_function(1000)\"\n    time = timeit.timeit(stmt, setup, number=1000)\n    print(f\"Average: {time/1000*1000:.2f}ms\")\n\n# Compare implementations\ndef compare_implementations():\n    implementations = {\n        'list_comprehension': '[x**2 for x in range(1000)]',\n        'map': 'list(map(lambda x: x**2, range(1000)))',\n        'for_loop': 'result = []\\nfor x in range(1000):\\n    result.append(x**2)'\n    }\n    \n    for name, code in implementations.items():\n        time = timeit.timeit(code, number=10000)\n        print(f\"{name}: {time:.4f}s\")\n\ncompare_implementations()\n# Output:\n# list_comprehension: 0.3421s\n# map: 0.4567s\n# for_loop: 0.5234s\n```\n\n## Performance Checklist\n\n- [ ] Profiled code to find bottlenecks\n- [ ] Optimized algorithm complexity (O(n²) → O(n log n) → O(n))\n- [ ] Added database indexes on frequently queried columns\n- [ ] Fixed N+1 query problems\n- [ ] Implemented caching for expensive operations\n- [ ] Used batch operations instead of loops\n- [ ] Applied lazy loading for large data\n- [ ] Used async/concurrent operations for I/O\n- [ ] Optimized memory usage (streaming, generators)\n- [ ] Benchmarked before and after optimization\n- [ ] Set up monitoring for production performance\n\n## When to Use This Skill\n\nActivate when:\n- User mentions \"slow\" or \"performance\"\n- User asks to \"optimize\" code\n- User mentions \"bottleneck\"\n- User asks about \"profiling\"\n- User wants to \"speed up\" code",
      "trigger_keywords": [
        "performance",
        "optimize",
        "slow",
        "bottleneck",
        "profiling",
        "speed up",
        "efficiency"
      ],
      "allowed_tools": [
        "Read",
        "Write",
        "Edit",
        "Bash",
        "Grep",
        "Glob"
      ]
    }
  ]
}
